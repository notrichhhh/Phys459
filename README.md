The code documentation and results of the codes used for Phys459's report

There is code that can run on it's own, and a series of codes that need to be run with shell codes.
The latter is saved in a sub-directory with example results.


